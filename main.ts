// main.ts

// Entry point for Mindmap AI Analyse App

// Placeholder: Mount UI to #app
document.getElementById('app')!.innerHTML = `
  <h1>Mindmap AI Analyse App</h1>
  <p>Velkommen! Appen loader...</p>
`;

// TODO: Import and initialize other modules (llm-api, mindmap, etc.) 